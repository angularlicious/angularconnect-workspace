import { ConfigurationContext } from './configuration-context';

describe('ConfigurationContext', () => {
  it('should create an instance', () => {
    expect(new ConfigurationContext()).toBeTruthy();
  });
});
